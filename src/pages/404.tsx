// This file exists to ensure Next.js generates pages-manifest.json during
// the build. An actual page route (not just _app.tsx) is required to trigger
// manifest creation. The App Router handles all real routes in this project.
export default function Custom404() {
  return null;
}
